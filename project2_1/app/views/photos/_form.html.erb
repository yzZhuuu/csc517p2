
<%= form_for ([@tour,@photo]) do |f| %>

  <div class="field">

    <p><%= f.label :image %></p>
    <p><%= f.file_field :image %></p>


  </div>

  <% form.hidden_field :user_id, value: current_user.id %>

  <div class="actions">
  <p><%= form.submit "add a image"%></p>
  </div>


<% end %>
